import { Box } from "@mui/material";

export default function Offert() {
  return (
    <Box
      style={{
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      hej
    </Box>
  );
}
